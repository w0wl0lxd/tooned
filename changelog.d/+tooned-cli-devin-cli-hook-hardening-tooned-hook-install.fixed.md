**tooned-cli:** Devin CLI hook hardening: `tooned hook install --devin` now writes a 5-second `timeout` on the generated hook command, coerces malformed hooks files to the expected object/array shape instead of silently no-oping, and uses `%APPDATA%\devin\config.json` on Windows without requiring a home directory. Tests now match Devin's real `PostToolUse` payload (no `hook_event_name`) and cover status, doctor, coexistence, and concurrent install atomicity.
