Codex `PostToolUse` hook output now emits both `continue: false` and `decision: "block"` alongside the `reason` field, ensuring the TOON text replaces the model-visible tool result rather than being passed through.