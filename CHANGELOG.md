# Changelog

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), [Semantic Versioning](https://semver.org/spec/v2.0.0.html). Fragments: `changelog.d/` (see `changelog.d/README.md`).

## [Unreleased]

### Known limitations

- Not yet tagged or published to crates.io.
- `--scope user|project` is Claude-Code-specific; with `--codex` it has no effect (`.codex-plugin/` always writes project-local) and `tooned` warns on stderr rather than ignoring silently.

<!-- towncrier release notes start -->
